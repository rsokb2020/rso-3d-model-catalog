kumuluzee:
  name: model-3d-catalog-service
  env:
    name: dev
  version: 1.0.0
  server:
    base-url: http://localhost:8080
    http:
      port: 8080
  datasources:
    - jndi-name: jdbc/Model3dMetadataDS
      connection-url: jdbc:postgresql://localhost:5432/model3d-metadata
      username: klemen
      password: rso6750
      max-pool-size: 20
  health:
    checks:
      data-source-health-check:
        type: liveness
        jndi-name: jdbc/Model3dMetadataDS
      consul-health-check:
        connection-url: http://52.151.200.181/ui/hashidc1/nodes/azure-consul-server-0/health-checks
  metrics:
    web-instrumentation:
      - name: models-endpoint
        url-pattern;: /v1/models/*
  config:
    consul:
      agent: http://52.151.200.181:80

rest-properties:
  maintenance-mode: false
  broken: false